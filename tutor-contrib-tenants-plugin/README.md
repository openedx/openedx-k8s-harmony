# Multi-domain setup

This tutor plugin uses eox-tenant django plugin to override Django settings for individual tenants, which means we can also override configuration variables that are not site-configuration aware.

## Usage


